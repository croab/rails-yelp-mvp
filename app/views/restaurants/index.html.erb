<% @restaurants.each do |restaurant| %>
  <div class="container mt-3">
    <h2><%= restaurant.name %></h2>
    <p><%= restaurant.category %></p>
    <%= link_to "See restaurant", restaurant_path(restaurant), class: 'btn btn-primary' %>
  </div>
<% end %>
